# Vowel
vowel = ['a', 'e', 'i', 'o', 'u']

# Counting the number of vowels
num = 0

# The main loop
for i in range(len(vowel)):
    num += s.count(vowel[i])

# Print the result
print("Number of vowels: {}".format(num))

